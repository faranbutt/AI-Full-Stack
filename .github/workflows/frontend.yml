name: Frontend App
on: 
    push: 
        branches: 
            - main
jobs:
    build-deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Get Code
              uses: actions/checkout@v4.1.0
            - name: List 
              run: ls
              
    